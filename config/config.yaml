# OliveWolf 数字人框架统一配置

# ===== 运行环境 =====
device: "cuda"            # cuda | mps | cpu
# 注意: LHM 全身3D后端仅支持 cuda

# ===== OpenAI 系 API =====
openai:
  api_key: "${OPENAI_API_KEY}"
  base_url: "https://api.openai.com/v1"
  # ASR
  asr_model: "whisper-1"
  # LLM
  llm_model: "gpt-4o"
  llm_temperature: 0.6
  llm_max_tokens: 800

# ===== TTS (ElevenLabs) =====
tts:
  provider: "elevenlabs"
  api_key: "${ELEVENLABS_API_KEY}"
  voice_id: "your_voice_id"        # 数字人音色ID
  model_id: "eleven_multilingual_v2"
  stability: 0.5
  similarity_boost: 0.75
  streaming: true                  # 流式合成，降低首字延迟

# ===== 数字人资产 =====
assets:
  # LivePortrait 头肩形象（源图，工装照）
  liveportrait_source: "assets/avatars/agent_female.jpg"
  # LHM 全身形象
  lhm_source: "assets/avatars/agent_full_body.jpg"
  # 待机动作模板
  idle_motion: "assets/motions/idle.pkl"

# ===== LivePortrait 渲染后端 =====
liveportrait:
  project_root: "../LivePortrait"   # 相对于 i5j_dh
  flag_use_half_precision: true
  flag_stitching: true
  flag_do_crop: true
  flag_pasteback: true
  output_fps: 25
  source_max_dim: 1280
  # 音频驱动参数
  audio2lip:
    frame_rate: 25                  # 与 output_fps 对齐
    smooth_window: 5                # 嘴部开合度平滑窗口
    energy_threshold: 0.02          # 静音能量阈值
    open_scale: 0.6                 # 最大开合度上限(防止嘴张过大)

# ===== LHM 渲染后端（离线） =====
lhm:
  project_root: "../LHM"
  model_name: "LHM-500M-HF"         # 平衡速度与质量
  render_fps: 30
  source_size: 336
  render_size: 288

# ===== 房产知识库 (Phase 3 RAG) =====
knowledge:
  enabled: false
  index_path: "assets/knowledge/index"
  chunks_path: "assets/knowledge/chunks"

# ===== 场景预设 =====
scenes:
  property_explainer:               # ① 产品/空间讲解员
    renderer: "lhm"                 # 全身3D
    llm_system_prompt: "你是专业数字人讲解员，负责介绍产品、空间、服务与关键卖点。语言亲和、重点突出。"
    motion: "assets/motions/walk_and_talk.pkl"
  consultant:                       # ② 7×24智能顾问
    renderer: "liveportrait"        # 实时头肩
    llm_system_prompt: "你是专业企业数字人顾问，回答要准确、简洁、可信。复杂问题引导转人工。"
  live_streamer:                    # ③ 直播主播
    renderer: "liveportrait"
    llm_system_prompt: "你是专业直播数字人主播，清晰介绍产品卖点，引导观众咨询或留资。"
  process_guide:                    # ④ 流程讲解员
    renderer: "lhm"
    llm_system_prompt: "你是专业流程讲解数字人，逐步讲解业务流程、注意事项与下一步操作。"
